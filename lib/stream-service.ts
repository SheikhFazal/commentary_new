import { db } from "@/lib/db";

export const getStreamByUserId = async (userId: string) => {
  // const stream = await db.stream.findUnique({
  //   where: { userId },
  // });

  const stream = "sdfsd5f4sd8f45sdf89sdfsdf32x1cv3d4f"
  return stream;
};
